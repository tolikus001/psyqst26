---
name: self-learning-auditor
description: Самообучающийся аудитор качества и базы знаний проекта. Анализирует код, фиксирует ошибки, обновляет реестр недопустимых паттернов и перепроверяет код перед любыми действиями.
---

# 🧠 Самообучающийся Аудитор Кода и Качества (Self-Learning Auditor)

Специализация: **Автоматический контроль качества, предотвращение повторных ошибок и непрерывное обучение на основе опыта разработки проекта.**

Агент обязан запускать проверку через этот скил **ДО** и **ПОСЛЕ** любых изменений в кодовой базе.

---

## 🔄 1. Цикл Непрерывного Обучения (Self-Learning Loop)

При каждом действии или анализе кода агент следует 4 шагам:

1. **Inspect (Инспекция)**: Проверить целевой код по Реестру известных ошибок (Раздел 2).
2. **Execute (Исполнение)**: Написать чистый, отказоустойчивый код с соблюдением всех стандартов.
3. **Verify (Верификация)**: Запустить чек-лист перепроверки (Раздел 3).
4. **Learn & Persist (Обучение)**: Если возникла новая ошибка, зафиксировать её в *Журнале уроков* этого скила, чтобы никогда больше её не повторять.

---

## 📚 2. Реестр Усвоенных Уроков и Недопустимых Ошибок

### 🎨 [УРОК 1] Базовый дизайн проекта — НЕИЗМЕНЯЕМЫЙ СТАНДАРТ
- **Правило**: Базовый дизайн взят строго из файла `baza/исходник миникурса/Псиквест_5.6.html` (стили в `theme.css`).
- **Палитра**: Фон `#F3F6F5` (`.app-shell`), Текст `#243139`, Вторичный `#68767C`, Акцент `#7F9B8E`, Морской `#45656D`, Линии `rgba(14,31,46,.10)`.
- **Запрет**: Запрещено самовольно менять палитру на темную, неоновую или стороннюю без прямого запроса пользователя.

### 📐 [УРОК 2] Центрирование заголовков и подзаголовков
- **Правило**: ВСЕ заголовки (`h1`, `h2`, `h3`, `h4`, `.question-title`, `.hero h1`) и подзаголовки (`.lead`, `.subtle`, `.eyebrow`, `.section-subtitle`, `p.subtitle`) **обязаны центрироваться по центру** (`text-align: center !important`).
- **Запрет**: Никаких заголовков с выравниванием по левому краю.

### 📱 [УРОК 3] Notibot Sandbox & Iframe Высота
- **Правило**: Использовать `min-h-[100dvh]` на `body` и контейнере `.app-shell`, `h-[100dvh]` на скроллящихся областях.
- **Запрет**: Строго запрещено `body { position: fixed }` и `overflow: hidden` на body (ломает `ResizeObserver` высоты Notibot и вызывает бесконечный цикл ресайза).
- **Запрет**: Не использовать устаревшие `100vh` или `h-screen`.

### 🛡 [УРОК 4] Ранний перехватчик сообщений Notibot
- **Правило**: В `<head>` каждой страницы обязан присутствовать ранний перехватчик `window.notibotInitData`:
```html
<script>
  window.notibotInitData = null;
  window.addEventListener('message', function(event) {
    if (event.data && event.data.type === 'NOTIBOT_INIT' && event.data.data) {
      window.notibotInitData = event.data.data;
    }
  });
</script>
```

### 📳 [УРОК 5] Безопасный Haptic Feedback
- **Правило**: Любые клики и взаимодействия с кнопками/ответами должны вызывать функцию `safeHaptic(type)` с 3 уровнями защиты (Notibot $\rightarrow$ Telegram WebApp $\rightarrow$ `navigator.vibrate` $\rightarrow$ silent catch).
- **Запрет**: Запрещен прямой вызов `window.Telegram.WebApp.HapticFeedback` без проверки на `undefined`.

### 🎬 [УРОК 6] Видеохостинг Kinescope и Потоковый Стриминг
- **Правило**: Все обучающие видео курса хостятся на **Kinescope** (`https://kinescope.io/embed/[ID]` или Kinescope CDN) в адаптивных 16:9 контейнерах.
- **CSP**: В `<meta http-equiv="Content-Security-Policy">` обязательны:
  - `frame-src 'self' https://kinescope.io https://*.kinescope.io;`
  - `media-src 'self' https: data: blob: https://*.kinescopecdn.net;`
  - `script-src 'self' 'unsafe-inline' https://kinescope.io https://*.kinescope.io;`

### 🚫 [УРОК 9] Полный запрет заглушек (No Stubs / No Placeholders)
- **Правило**: В коде, разметке и скриптах категорически **запрещены временные заглушки, моки, фейковые данные (Lorem Ipsum, placeholder.png, dummy IDs)**.
- **Причина**: Заглушки легко забыть перед переносом в прод, они ломают боевые Notibot-транзакции и требуют долгой ручной зачистки.
- **Требование**:
  - Использовать реальные тексты из транскрибаций автора.
  - Использовать настоящие медиа-ссылки (`lesson0.mp4`, `meditation_audio.MP3`).
  - Реальные Notibot-обработчики (`window.notibot.openProduct`, `window.notibot.sendMessage`).
  - Реальные пользовательские имена из Telegram (`Telegram.WebApp.initDataUnsafe.user`) без захардкоженных тестовых имён.

---

### 📦 [УРОК 10] Защита от обрезания разметки (Full Body Integrity)
- **Ошибка**: При редактировании `<head>` или стилей в больших файлах тело страницы (`<body>`) может быть случайно усечено.
- **Правило**: Любая модификация файлов обязана сохранять полный объём разметки `<body>...</body>`. Аудитор проверяет наличие закрывающего `</body>` и интерактивных карточек.

### 🔗 [УРОК 11] Бесшовная маршрутизация экранов Notibot (`openScreen` вместо прямых ссылок)
- **Ошибка**: Прямые ссылки `<a href="02_landing_partner.html">` или `window.location.href = '04_lesson2.html'` внутри Telegram WebApp вызывают всплывающее окно *"Перейти по ссылке"* или открывают пустую страницу в песочнице Notibot.
- **Правило**: Все переходы между страницами курса внутри кнопок и ссылок выполняются строго через `openScreen('target.html')` и `href="javascript:void(0);" onclick="openScreen('target.html'); return false;"`.
- **Механизм**: `openScreen` сопоставляет имя файла с ID статьи Notibot и вызывает `window.notibot.openArticle(id)`, обеспечивая мгновенное переключение без подтверждений Telegram.

### 🎨 [УРОК 12] Гармонизация элементов шапки (Avatar & Strategy Badge)
- **Правило**: Аватар пользователя и бейдж курса/стратегии в шапке используют единую цветовую систему (морской оттенок `#45656D`, фон `rgba(69, 101, 109, 0.10)` и тонкий контур).

### ⏸️ [УРОК 15] Обязательная кросс-браузерная авто-пауза видео при переходе к практике и аудио
- **Правило**: При ЛЮБОМ переходе пользователя к интерактивной практике (`goToStep2()`, `goToInteractive()`, `goToPractice()`), навигации между экранами (`openScreen()`, `safeNavigate()`, `goToStep1()`, `goToVideo()`) или запуске аудиоплеера медитации воспроизведение видео Kinescope (iframe) и аудио ДОЛЖНО быть немедленно остановлено через вызов `pauseVideo()`.
- **Запрет**: Категорически запрещено оставлять играющее фоном видео или аудио при открытии практических шагов или следующего экрана.
- **Реализация**:
```javascript
function pauseVideo() {
  const iframes = document.querySelectorAll('iframe');
  iframes.forEach(iframe => {
    if (iframe) {
      if (iframe.contentWindow) {
        try {
          iframe.contentWindow.postMessage(JSON.stringify({ event: 'pause', command: 'pause' }), '*');
          iframe.contentWindow.postMessage({ type: 'pause', command: 'pause' }, '*');
          iframe.contentWindow.postMessage({ type: 'player:pause' }, '*');
          iframe.contentWindow.postMessage(JSON.stringify({ type: 'player:pause' }), '*');
          iframe.contentWindow.postMessage({ event: 'command', command: 'pause' }, '*');
          iframe.contentWindow.postMessage('{"method":"pause"}', '*');
          iframe.contentWindow.postMessage(JSON.stringify({ method: 'pause' }), '*');
        } catch(e) {}
      }
      try {
        const currentSrc = iframe.src;
        if (currentSrc && currentSrc.includes('kinescope.io')) {
          iframe.src = currentSrc;
        }
      } catch(e) {}
    }
  });
  const audios = document.querySelectorAll('audio');
  audios.forEach(a => { try { a.pause(); } catch(e){} });
}
```

### 🏷️ [УРОК 16] Обязательное наличие стилей интерактивных элементов в theme.css (`.option-chip`, `.check-circle`)
- **Ошибка**: Использование кастомных классов вариантов выбора (например, `.option-chip` и `.check-circle`) без описания в дизайн-системе приводит к рендеру нестилизованного текста без индикации нажатия и застреванию пользователя на шаге.
- **Правило**: Все элементы выбора обязаны иметь полное оформление в `theme.css` (белая карточка со скруглением 16px, граница, тень, активное состояние `.selected` с морским/акцентным фоном и анимированной галочкой).

### 🎚️ [УРОК 17] Градиентные шкалы и ползунки (Green-to-Red Range Sliders)
- **Ошибка**: Стандартные браузерные слайдеры (`<input type="range">`) имеют системный синий цвет и плохую читаемость подписей крайних значений.
- **Правило**: Интерактивные шкалы оценки состояния обязаны использовать выразительный градиент: от спокойного зелёного (`#2F7D59` / 0%) через янтарный (`#E59A5A` / 50%) к тревожному красному (`#B42318` / 100%), бейдж значения динамически меняет цвет, а подписи крайних состояний строго разнесены (`justify-content: space-between`).

### 🚫 [УРОК 19] Запрет упоминания/предложения сервиса Netlify без прямого запроса
- **Запрет**: Категорически запрещено упоминать, предлагать или навязывать публикацию/деплой через сторонний сервис Netlify, если пользователь явно и прямо сам не спросил об этом.
- **Правило**: При обсуждении публикации и размещения сайта ориентироваться исключительно на штатную инфраструктуру проекта (Amvera / Notibot / локальные решения), не упоминая Netlify.

### 🔍 [УРОК 20] Обязательный аудит целостности перед переносом в Notibot (Pre-Notibot Transfer Audit)
- **Триггер**: Когда пользователь сообщает «переношу в нотибот», «загружаю в нотибот» или просит подготовить файлы к переносу.
- **Обязательные действия агента**:
  1. Провести полный аудит всех 14 HTML-файлов на **полное отсутствие заглушек** (`TODO`, `dummy`, `mock`, `placeholder.png`, тестовых данных).
  2. Проверить **все ссылки и медиа-ID**: видеоплееры Kinescope, аудиопотоки Amvera (`.MP3`), Notibot Product IDs (2400 / 5000 / 10000), Notibot Article IDs.
  3. Проверить автономность разметки: inline CSS `theme.css`, base64 background/illustrations, Notibot Bridge SDK в `<head>`.
  4. Запустить синтаксический и runtime аудит: `node scripts/global-audit.js && node scripts/repair-all.js`.
  5. Предоставить пользователю чёткий отчёт со списком проверенных файлов и прямыми ссылками.

### 🛡️ [УРОК 21] Комплексный аудит безопасности и защита от утечки секретов
- **Запрет**: Категорически запрещено хранить в клиентском коде секретные ключи (`API_KEY`, `secret`, токены доступа).
- **Правило**:
  1. Все внешние вызовы ИИ выполняются через защищённый бэкенд на Amvera (`process.env.POLZA_API_KEY`), клиентский код получает только готовый JSON.
  2. В каждом HTML-файле настроен строгий заголовок `Content-Security-Policy` с белым списком разрешённых доменов (`telegram.org`, `list.notibot.ru`, `kinescope.io`, `*.amvera.io`, `fonts.googleapis.com`, `cdn.jsdelivr.net`).
  3. Перед коммитами агент запускает автоматический сканер `node scripts/security-audit.js`.

---

## 🔍 3. Чек-Лист Перепроверки Кода (Audit Protocol)

Перед сохранением любого файла агент проверяет:

- [ ] **1. Дизайн**: Соответствует ли разметка палитре и токенам `theme.css`?
- [ ] **2. Центрирование**: Все ли заголовки и подзаголовки имеют `text-align: center`?
- [ ] **3. Без заглушек**: Проверено ли отсутствие `TODO`, `dummy`, `Lorem ipsum`, `placeholder.png`, тестовых моков?
- [ ] **4. Персонализация**: Работает ли извлечение имени и аватара из Telegram / Notibot?
- [ ] **5. Маршрутизация**: Используется ли `openScreen('...')` вместо прямых `href="..."` и `window.location.href`?
- [ ] **6. Авто-пауза видео**: Вызывается ли `pauseVideo()` при переходе к практике, старте аудио и переключении экранов?
- [ ] **7. Haptic**: Обернуты ли клики в `safeHaptic`?
- [ ] **8. Стили интерактивных элементов**: Описаны ли `.option-chip`, `.check-circle`, `.touch-range` в CSS?
- [ ] **9. CSP**: Разрешены ли домены `kinescope.io`, `*.notibot.ru`, `telegram.org`, `fonts.googleapis.com`?
- [ ] **10. Мобильная адаптивность**: Тестируется ли экран на ширину 320px–448px?
- [ ] **11. Синтаксис & Целостность**: Проверен ли код через `node scripts/repair-all.js` и наличие всех секций `<body>`?
- [ ] **12. Локация**: Сохранен ли файл строго в директории `out/client/`?
- [ ] **13. Предпереносной аудит Notibot**: Проверены ли медиа-ссылки и отсутствие заглушек перед переносом в Notibot?
- [ ] **14. Безопасность & Отсутствие ключей**: Проверен ли код сканером `node scripts/security-audit.js` на утечки токенов и уязвимости?
- [ ] **15. Отчёт пользователю**: Указан ли полный список изменённых файлов с ссылками?

---

## 📝 4. Журнал Обучения и Новых Уроков

| Дата | Событие / Ошибка | Выученный урок и решение |
|---|---|---|
| 2026-08-14 | Установка базового дизайна | Дизайн зафиксирован по `Псиквест_5.6.html` как неизменяемый стандарт. |
| 2026-08-14 | Выравнивание заголовков | Введено обязательное правило: все заголовки и подзаголовки строго центрируются. |
| 2026-08-14 | Запрет заглушек | Полный запрет моков, заглушек и фейковых данных, которые мешают переносу. |
| 2026-08-14 | Telegram Персонализация | Автоматическое извлечение имени и аватара пользователя при входе в мини-приложение. |
| 2026-08-15 | Обрезание разметки уроков | Автоматический контроль целостности `<body>` при обновлении стилей/скриптов. |
| 2026-08-15 | Всплывающее окно Telegram при навигации | Замена прямых ссылок `<a>` и `window.location` на `openScreen()` с `window.notibot.openArticle`. |
| 2026-08-15 | Разнобой оттенков в шапке | Приведение фона аватара и плашки курса к единому тону `#45656D`. |
| 2026-08-16 | Видео продолжало играть при переходе к практике | Внедрена кросс-браузерная функция `pauseVideo()` с сбросом потока `iframe.src` и авто-паузой при старте медитации. |
| 2026-08-16 | Нестилизованные варианты выбора в практике 4-го урока | Все интерактивные карточки (`.option-chip`, `.check-circle`) добавлены в дизайн-систему `theme.css`. |
| 2026-08-16 | Синие системные слайдеры и слипшиеся метки во 2-м уроке | Реализованы фирменные градиентные дорожки (зелёный -> красный) с динамическими бейджами и разнесёнными метками. |
| 2026-08-16 | Неверный Kinescope ID 4-го урока | Верифицирован и внедрён точный видео-ID `naNZuQTimmoFoUCi38tSJA`. |
| 2026-08-16 | Упоминание стороннего хостинга Netlify | Строгий запрет предлагать или упоминать сервис Netlify без прямого запроса пользователя. |
| 2026-08-16 | Перенос проекта в Notibot Articles | Обязательный аудит на отсутствие любых заглушек, валидность Kinescope, Amvera аудио и Notibot Product IDs. |
| 2026-08-16 | Защита от утечки ключей и уязвимостей | Автоматический аудит `scripts/security-audit.js` на отсутствие секретов, XSS и соответствие CSP. |
